<% if !user_signed_in? %>
<%= link_to "Sign in", new_user_session_path, class: 'btn btn-success' %> 
 or 
<%= link_to "Sign up", new_user_registration_path, class: 'btn btn-success' %>
 to get walking! 
<br>
<br>
<br>
<% end %>



<p>Find a walker by posting a <%= link_to 'walker needed', new_job_path, class: 'btn btn-primary' %> item below</p>

<h1>Pups in need of a walk</h1>

<% @jobs.each do |job| %>
<%= render partial: 'job', locals: { job: job } %>
<% end %>